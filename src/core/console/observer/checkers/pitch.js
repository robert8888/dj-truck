import STATUS from  "./../STATUS";

const responseTempalte = {
    status : STATUS,
    chanel: "A or B",
    prevValue: 0,
    currentValue: 0,  
}

export default function checkPitchChange(prev, curent){

}